
<h1>Showing NBA Game</h1>
 
<table border="1">
  <tr>
    <% @stats.each do |col, val| %>
    	<th><%= col %></th>
    <% end %>
  </tr>
 
    <tr>
    	<% @stats.each do |col, val| %>
    		<td align="center"><%= val %></td>
    	<% end %>
    </tr>
</table>

<%= link_to 'Edit', edit_nbagame_path(@nbagame) %>
<%= link_to 'Back', nbagames_path %>
